# M0ProtocolMockClient (Android)

Kotlin/JVM mirror of the iOS `M0ProtocolMockClient` contract package.

## Scope

1. Protocol envelope and event models.
2. Resume request model.
3. Replay planner.
4. Replay cursor store interface + in-memory implementation.

## Run Tests

```bash
cd /mnt/c/sriinnu/personal/Kaala-brahma/terminal/apps/android/M0ProtocolMockClient
gradle test
```
